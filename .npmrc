# Local npm config for this project.
# NOTE: the sandboxed environment injects npm_config_allow_scripts as an env
# var; npm 11.17 rejects that CLI/env layer in project-scoped installs, so the
# effective policy lives in package.json#allowScripts instead.
